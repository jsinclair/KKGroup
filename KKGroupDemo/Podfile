# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'KKGroupDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KKGroupDemo
  pod 'SwiftLint'

  target 'KKGroupDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'KKGroupDemoUITests' do
    # Pods for testing
  end

end
